# Static deployment is handled by static.yml
# This file is kept for reference only and is disabled
name: (Disabled) Legacy Deploy

on:
  workflow_dispatch: # manual only, not on push

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment is handled by static.yml"

